# 工位调度大师 - 项目业务梳理文档

## 一、项目概述

这是一个基于 **React 18 + TypeScript + Vite** 的前端调度模拟小游戏，模拟场馆/展厅工位管理场景。玩家扮演调度员，在限定时间内统筹安排各工位的**清洁**与**补料**任务，应对突发事件，争取获得高分。

**技术栈：**
- 框架：React 18 + TypeScript
- 构建：Vite 6
- 状态管理：Zustand 5
- 样式：Tailwind CSS 3
- 图标：lucide-react
- 数据存储：localStorage（无后端）

---

## 二、目录结构

```
src/
├── components/          # UI 组件
│   ├── ControlPanel.tsx     # 游戏控制面板（暂停/继续/下一场）
│   ├── Empty.tsx            # 空状态占位
│   ├── EventNotification.tsx # 突发事件弹窗通知
│   ├── StationPanel.tsx     # 工位状态面板
│   ├── TaskList.tsx         # 任务清单（支持拖拽排序）
│   └── TimerBar.tsx         # 倒计时进度条
├── data/
│   └── levels.ts            # 关卡配置数据 & 工位初始化
├── hooks/
│   └── useTheme.ts          # 主题 hook（预留）
├── lib/
│   └── utils.ts             # 通用工具（clsx 合并）
├── pages/                   # 页面组件
│   ├── GamePage.tsx         # 游戏主界面
│   ├── Home.tsx             # 首页（未使用/重定向）
│   ├── LevelSelect.tsx      # 关卡选择
│   ├── MainMenu.tsx         # 主菜单
│   ├── ReplayPage.tsx       # 复盘页面
│   ├── ResultPage.tsx       # 结算页面
│   ├── ScoresPage.tsx       # 成绩记录
│   └── TutorialPage.tsx     # 游戏教程
├── store/
│   └── gameStore.ts         # Zustand 全局状态（核心逻辑中枢）
├── types/
│   └── index.ts             # TypeScript 类型定义
└── utils/
    ├── scoreCalculator.ts   # 得分计算引擎
    └── storage.ts           # localStorage 持久化
```

---

## 三、核心数据结构

### 3.1 主要枚举类型

| 类型 | 定义 | 说明 |
|------|------|------|
| `GameStatus` | `'idle' \| 'playing' \| 'paused' \| 'finished'` | 游戏运行状态 |
| `TaskType` | `'clean' \| 'refill'` | 任务类型：清理 / 补料 |
| `TaskPriority` | `'high' \| 'medium' \| 'low'` | 任务优先级：高 / 中 / 低 |
| `TaskStatus` | `'pending' \| 'in_progress' \| 'completed'` | 任务执行状态 |
| `MaterialStatus` | `'full' \| 'partial' \| 'empty' \| 'missing'` | 物料状态：充足/不足/空缺/缺失 |
| `EventType` | `'missing_material' \| 'severe_dirt' \| 'guide_delay' \| 'early_arrival'` | 突发事件类型 |
| `PageType` | `'menu' \| 'game' \| 'result' \| 'tutorial' \| 'scores' \| 'levelSelect' \| 'replay'` | 页面路由 |

### 3.2 核心实体

**工位 Station**（[types/index.ts#L33-L41](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/types/index.ts#L33-L41)）
- `id` / `name`：工位编号和名称（接待台、展示区A、互动区B 等）
- `cleanliness`：清洁度（0-100），初始随机 20-60
- `materialStatus`：物料状态
- `isCritical`：是否关键工位（1号工位默认为关键工位，权重加倍）
- `isDirty`：是否重度污损（由事件触发，清洁时间加倍）
- `currentTaskId`：当前执行任务ID（预留，实际通过 tasks 状态查询）

**任务 Task**（[types/index.ts#L43-L51](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/types/index.ts#L43-L51)）
- `id`：格式 `{type}-{stationId}[-{timestamp}]`（自动生成 vs 手动添加）
- `type` / `stationId` / `priority`：类型、所属工位、优先级
- `duration`：预计耗时（秒）——清理默认10秒，污损20秒；补料固定5秒
- `progress`：执行进度（0-100）
- `status`：pending → in_progress → completed

**关卡 Level**（[types/index.ts#L23-L31](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/types/index.ts#L23-L31)）
- `id` / `name` / `description`：关卡编号、名称、描述
- `duration`：关卡时长（秒）：90→80→70→60（逐级递减，难度增加）
- `stationCount`：工位数量（3→4→5→5）
- `events`：预设突发事件列表（按时间触发）
- `unlockScore`：解锁下一关所需分数（60/70/75/80）

**得分 Score**（[types/index.ts#L53-L62](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/types/index.ts#L53-L62)）
- 包含 cleanRate、refillAccuracy、delayDuration、stationUtilization 四个维度
- totalScore：加权总分（0-100）
- stars：星级（0-3）：≥90三星，≥70两星，≥50一星

**游戏状态 GameState**（[types/index.ts#L64-L82](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/types/index.ts#L64-L82)）
- `currentPage` / `currentLevelId`：当前页面和关卡
- `gameStatus` / `timeRemaining`：游戏状态和剩余时间
- `stations` / `tasks` / `activeEvents`：工位、任务、事件列表
- `guideDelayTime` / `earlyArrivalTime`：事件带来的时间修正（+10s/-15s）
- `taskTimeline` / `eventTimeline` / `removedTaskSnapshots`：用于复盘的时间线记录
- `currentReplayTimestamp` / `replayReturnPage`：复盘导航状态

---

## 四、页面流程与用户操作路径

### 4.1 页面导航图

```
主菜单 (MainMenu)
  ├── 开始游戏 → 关卡选择 (LevelSelect)
  │                 └── 点击关卡 → 游戏中 (GamePage)
  │                                   ├── 时间到/主动结束 → 结算页 (ResultPage)
  │                                   │                       ├── 重新挑战
  │                                   │                       ├── 下一关（解锁后）
  │                                   │                       ├── 查看复盘 (ReplayPage)
  │                                   │                       ├── 选择关卡
  │                                   │                       └── 返回主菜单
  │                                   ├── 暂停/继续
  │                                   └── 返回菜单
  ├── 游戏教程 (TutorialPage) → 开始游戏
  └── 成绩记录 (ScoresPage)
        ├── 点击历史记录 → 复盘页 (ReplayPage)
        └── 重置数据（确认弹窗）
```

页面切换完全通过 Zustand store 的 `navigateTo(page)` 方法驱动，在 [App.tsx#L10-L35](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/App.tsx#L10-L35) 中根据 `currentPage` 渲染对应页面组件。

### 4.2 游戏主界面布局

[GamePage.tsx](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/pages/GamePage.tsx) 采用三栏布局：

```
┌─────────────────────────────────────────────────────────┐
│  [突发事件通知 - 右上角弹窗]                              │
├─────────────────────────────────────────────────────────┤
│  关卡标题 + 描述                                         │
│  [倒计时进度条 TimerBar]                                 │
├──────────────────┬──────────────────────────────────────┤
│                  │  [任务清单 TaskList]                  │
│  [工位面板        │   ├── 进行中任务                      │
│   StationPanel]  │   ├── 待执行任务（可拖拽排序）          │
│                  │   └── 已完成任务                      │
│  显示每个工位的   ├──────────────────────────────────────┤
│  清洁度/物料状态  │  [控制面板 ControlPanel]              │
│  触发补料按钮     │   暂停 | 开始下一场 | 返回菜单         │
└──────────────────┴──────────────────────────────────────┘
```

### 4.3 用户操作汇总

| 操作 | 位置 | 效果 |
|------|------|------|
| 拖拽任务卡片 | 待执行任务列表 | 调整任务执行顺序 |
| 修改优先级下拉框 | 任务卡片右侧 | 仅改变视觉标记（**注意：当前不影响调度顺序**） |
| 点击 ✕ 删除任务 | 待执行任务卡片 | 移除任务（记录到 removedTaskSnapshots 用于复盘） |
| 点击「触发补料」 | 工位卡片 | 手动添加一个补料任务到队列 |
| 点击「暂停/继续」 | 控制面板 | 暂停/恢复游戏计时和任务执行 |
| 点击「开始下一场」 | 控制面板 | 提前结束本局，标记 isNextSessionReady=true |
| 点击事件通知 | 右上角弹窗 | 关闭通知（也会3秒自动消失） |

---

## 五、游戏状态流转

### 5.1 游戏生命周期

```
                 startLevel(levelId)
  ┌──────┐    ─────────────────────→   ┌─────────┐
  │ idle │                            │ playing │ ←───┐
  └──────┘                            └────┬────┘     │
       ↑                                   │ tick()   │ resumeGame()
       │ resetGame()              ┌────────┴────────┐ │
       │                         │                 │ │
       │                    pauseGame()       时间耗尽/主动结束
       │                         │                 │ │
       │                         ↓                 ↓ │
  ┌──────┐                   ┌────────┐       ┌──────────┐
  │ menu │ ←── navigateTo ── │ paused │       │ finished │
  └──────┘                   └────────┘       └────┬─────┘
                                                   │
                                                   ↓
                                            结算页 (result)
```

核心逻辑集中在 [gameStore.ts](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/store/gameStore.ts) 的 `tick()` 方法中，由 [GamePage.tsx#L14-L22](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/pages/GamePage.tsx#L14-L22) 的 `setInterval` 每秒驱动一次。

### 5.2 每秒 Tick 逻辑（核心循环）

[tick()](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/store/gameStore.ts#L109-L218) 方法每帧执行以下步骤：

```
1. 检查游戏状态是否为 playing，否则跳过
2. 计算实际剩余时间 = timeRemaining - earlyArrivalTime + guideDelayTime
   → 若 ≤ 0，调用 finishGame() 结束游戏
3. 检查突发事件触发：
   → 遍历 activeEvents，若 elapsed ≥ event.time 且未触发
   → 根据事件类型修改状态（见第六节）
   → 记录到 eventTimeline
4. 任务调度：
   → 最多同时执行 maxParallelTasks = 2 个任务
   → 同一工位不能并行执行多个任务
   → 从待执行队列中按顺序取可用任务启动
   → 记录启动到 taskTimeline
5. 任务进度推进：
   → 进行中任务 progress += 100/duration（每秒推进）
   → 进度满100%则标记完成，更新工位状态
   → 清理完成：cleanliness=100, isDirty=false
   → 补料完成：materialStatus='full'
   → 记录完成到 taskTimeline
6. 更新 timeRemaining（-1秒）
```

---

## 六、任务系统

### 6.1 任务生成

**初始任务生成**（[gameStore.ts#L59-L82](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/store/gameStore.ts#L59-L82)）在 `startLevel()` 时自动执行：

- **清理任务**：工位 cleanliness < 80% 时自动生成
  - ID 格式：`clean-{stationId}`
  - 关键工位优先级为 high，其余为 medium
  - 时长：isDirty ? 20秒 : 10秒
- **补料任务**：工位 materialStatus ≠ 'full' 时自动生成
  - ID 格式：`refill-{stationId}`
  - 物料状态为 'empty' 时优先级 high，其余为 medium
  - 时长：固定 5秒

**手动补料任务**：玩家点击工位卡片的「触发补料」按钮，通过 `addTask()` 添加：
- ID 格式：`refill-{stationId}-{Date.now()}`（带时间戳避免重复）
- 清洁度 < 30% 时优先级 high，否则 medium

**任务防重复**：`addTask()` 和 `triggerRefill()` 都会检查是否已存在同类型同工位的未完成任务。

### 6.2 任务调度规则

[tick() 中的调度逻辑](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/store/gameStore.ts#L158-L183)：

1. 并发上限：最多 **2个任务** 同时执行
2. 工位互斥：同一工位同时只能有一个任务在执行
3. **调度顺序**：按 tasks 数组中的排列顺序依次选取（即待执行列表的顺序）
4. 玩家通过**拖拽排序**改变待执行任务的顺序，从而影响调度优先级

> ⚠️ **注意**：`priority` 字段（high/medium/low）当前仅用于 UI 颜色标记，在 `tick()` 调度逻辑中**并未实际参与排序**。这意味着修改优先级下拉框不会改变任务执行顺序，只有拖拽排序才会影响实际调度。这是一个潜在的功能缺陷/待完善点。

### 6.3 任务交互

- **拖拽排序**：[TaskList.tsx#L132-L147](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/components/TaskList.tsx#L132-L147) 使用 HTML5 Drag & Drop API，仅对待执行（pending）任务生效
- **删除任务**：`removeTask()` 会将任务快照保存到 `removedTaskSnapshots`，供复盘使用
- **已删除任务不参与得分计算**，但会在复盘中显示为「已删除任务」

---

## 七、突发事件系统

### 7.1 事件类型与效果

| 事件类型 | 触发效果 | 计分影响 |
|----------|----------|----------|
| `missing_material` 材料缺失 | 指定工位 materialStatus 变为 `'missing'`，需要补料 | 间接影响补料准确率 |
| `severe_dirt` 重度污损 | 指定工位 isDirty=true，**已有清理任务 duration 加倍**；新清理任务默认20秒 | 间接影响清理完成率和时间 |
| `guide_delay` 讲解员延迟 | guideDelayTime = 10秒，实际可用时间+10秒 | 减少延误扣分 |
| `early_arrival` 观众提前到达 | earlyArrivalTime = 15秒，实际可用时间-15秒 | 增加延误风险 |

### 7.2 事件触发机制

- 事件在关卡配置 [levels.ts](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/data/levels.ts) 中预设，每个事件有 `time` 字段（触发时间点，单位秒）
- 在 `tick()` 中判断 `elapsed >= event.time` 时触发，且只触发一次（`triggered` 标记）
- 触发后通过 [EventNotification.tsx](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/components/EventNotification.tsx) 在右上角弹出通知卡片
- 通知3秒后自动消失，玩家也可点击手动关闭

### 7.3 时间修正公式

实际剩余时间计算（[TimerBar.tsx#L10](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/components/TimerBar.tsx#L10) 和 [gameStore.ts#L120](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/store/gameStore.ts#L120)）：

```
actualTimeRemaining = timeRemaining - earlyArrivalTime + guideDelayTime
```

- 以关卡2为例：80秒基础，第30秒 early_arrival（-15s），第45秒 guide_delay（+10s）
- 事件叠加后净效果为 -5秒，相当于总可用时间变为75秒
- 当 actualTimeRemaining ≤ 0 时游戏结束

### 7.4 各关卡事件配置

| 关卡 | 名称 | 时长 | 工位数 | 事件 |
|------|------|------|--------|------|
| 1 | 新手入门 | 90s | 3 | 无 |
| 2 | 材料危机 | 80s | 4 | 50s时 2号工位材料缺失 |
| 3 | 清洁挑战 | 70s | 5 | 40s时 3号工位重度污损 |
| 4 | 双重压力 | 60s | 5 | 30s观众提前(-15s)，45s讲解延迟(+10s) |

---

## 八、得分系统

### 8.1 得分计算公式

[calculateScore()](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/utils/scoreCalculator.ts#L12-L37) 加权公式：

```
totalScore = cleanRate × 0.30
           + refillAccuracy × 0.25
           + (100 - delayScore) × 0.25
           + stationUtilization × 0.20
```

### 8.2 四个维度详细计算

**① 清理完成率（cleanRate）——权重 30%**（[scoreCalculator.ts#L39-L59](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/utils/scoreCalculator.ts#L39-L59)）
- 关键工位权重 ×2，普通工位权重 ×1
- 关键工位 cleanliness ≥ 90% 得满分，否则按 cleanliness/90 比例
- 普通工位 cleanliness ≥ 80% 得满分，否则按 cleanliness/80 比例
- 最终按加权平均换算为百分制

**② 补料准确率（refillAccuracy）——权重 25%**（[scoreCalculator.ts#L61-L72](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/utils/scoreCalculator.ts#L61-L72)）
- 两个子指标取平均：
  - 补料任务完成比例 = 已完成补料任务数 / 总补料任务数 × 100
  - 物料充足比例 = 物料为 full 的工位数 / 总工位数 × 100
- 没有补料任务时默认为 100 分

**③ 延误扣分（delayScore）——权重 25%**（[scoreCalculator.ts#L74-L79](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/utils/scoreCalculator.ts#L74-L79)）
- 该项以 `(100 - delayScore)` 计入总分，延误越多扣分越多
- **主动提前结束**（点击「开始下一场」）：delayDuration = 0，delayScore = 0（不扣分）
- **时间耗尽结束**：delayDuration = elapsedTime + 10（秒），delayScore = min(100, delayDuration × 2)
  - 即每超时1秒扣2分，最多扣100分（即该维度得0分）

> ⚠️ **注意**：当前 delayDuration 的计算逻辑存在一定模糊性。时间耗尽时 elapsedTime 已经超过 level.duration（因为有 earlyArrivalTime 修正），delayDuration 取值为 `level.duration - state.timeRemaining + 10`，这大约等于已过时间+10秒。由于实际结束由 actualTimeRemaining ≤ 0 触发，此时 timeRemaining 已经很小（≤ earlyArrivalTime - guideDelayTime），delayDuration 通常为较大值。

**④ 工位利用率（stationUtilization）——权重 20%**（[scoreCalculator.ts#L81-L109](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/utils/scoreCalculator.ts#L81-L109)）
- 统计至少完成过一个任务的工位比例
- 关键工位权重 ×2，普通工位权重 ×1
- 鼓励覆盖更多工位，不只是处理关键工位

### 8.3 星级评定

| 总分 | 星级 |
|------|------|
| ≥ 90 | ★★★ 三星 |
| ≥ 70 | ★★ 两星 |
| ≥ 50 | ★ 一星 |
| < 50 | 无星 |

### 8.4 关卡解锁

- 初始仅解锁关卡1
- 某关 totalScore ≥ 该关 unlockScore（60/70/75/80）时，自动解锁下一关
- 解锁逻辑在 [storage.ts#L56-L62](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/utils/storage.ts#L56-L62) 的 saveScore 中处理

---

## 九、数据持久化

### 9.1 存储结构

使用 `localStorage`，key 为 `station-scheduler-save`，数据结构 [LocalSaveData](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/types/index.ts#L84-L89)：

```typescript
{
  unlockedLevels: number[];      // 已解锁关卡ID列表，默认[1]
  bestScores: Record<number, Score>;  // 各关卡最高分
  scoreHistory: Score[];         // 历史得分记录（最近50条）
  replayData: ReplayData[];      // 复盘数据（最近50条）
}
```

### 9.2 存储操作（[storage.ts](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/utils/storage.ts)）

| 方法 | 功能 |
|------|------|
| `getSaveData()` | 读取全部存档 |
| `saveScore(score)` | 保存得分：更新 bestScores、添加到 history、检查解锁 |
| `saveReplayData(replay)` | 保存复盘数据 |
| `getReplayByTimestamp(ts)` | 按时间戳查询复盘数据 |
| `resetSaveData()` | 重置所有数据（在成绩页可操作） |

---

## 十、复盘系统

### 10.1 复盘数据采集

游戏结束时 [finishGame()](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/store/gameStore.ts#L289-L395) 会构建 [ReplayData](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/types/index.ts#L119-L133)：

- **事件时间线**：从 eventTimeline 还原每个事件的 triggeredAtElapsed
- **任务时间线**：从 taskTimeline 和 removedTaskSnapshots 还原每个任务的 startedAt/completedAt/removedAt
- **工位最终状态**：记录游戏结束时各工位的 cleanliness、materialStatus 等
- **得分构成**：保存四个维度的具体数值

### 10.2 复盘页面内容

[ReplayPage.tsx](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/pages/ReplayPage.tsx) 展示五个板块：

1. **得分概览**：本局总分 + 与历史最佳对比（差距）
2. **事件触发时间线**：按时间排序展示所有突发事件
3. **任务完成顺序**：分为已完成/未完成/已删除三类，带开始/完成时间戳
4. **工位最终状态**：各工位清洁度进度条和物料状态标签
5. **总分构成对比**：四个维度与最佳成绩的差值比较（绿色+表示优于最佳，红色-表示落后）

复盘入口有两个：
- 结算页的「查看复盘」按钮
- 成绩记录页点击历史记录条目（有复盘数据的条目显示 📊 图标）

---

## 十一、关键设计细节与注意事项

### 11.1 已确认的设计特性

- **并发上限**：2个任务同时执行，不可配置
- **工位互斥**：同一工位不能同时执行两个任务
- **关键工位**：1号工位（接待台）始终是关键工位，评分权重加倍
- **初始随机性**：工位初始清洁度和物料状态随机生成（cleanliness 20-60，materialStatus 在 partial/empty/partial 中随机），每局体验略有不同
- **时间暂停**：暂停时 setInterval 不停止，但 tick() 内检查 gameStatus !== 'playing' 直接返回，逻辑安全

### 11.2 需要注意的潜在问题/待优化点

1. **优先级字段未生效**：`changeTaskPriority()` 只修改了 priority 字段的值，但 `tick()` 调度时没有按 priority 排序，仅按数组顺序取任务。目前只有拖拽排序能真正改变执行顺序。优先级下拉框的修改不会影响游戏结果，仅改变颜色显示。

2. **缺少清理任务手动添加**：目前补料可手动触发，但清理任务只有初始化时自动生成，游戏中无法手动添加清理任务（即使工位被事件污损后，已有的清理任务时长会被加倍，但如果原任务已完成则不会自动重新生成清理任务）。

3. **任务ID冲突风险**：自动生成的初始任务ID为 `clean-{id}` / `refill-{id}`，手动添加的补料带时间戳。若初始任务被删除后再手动添加，不会冲突；但同一类型同一工位只能有一个未完成任务的检查是存在的，因此不会重复添加。

4. **delayDuration 计算逻辑**：时间耗尽时的 delayDuration 计算方式为 `elapsedTime + 10`，但由于实际结束受 earlyArrivalTime/guideDelayTime 影响，这个值的语义是「已用时间+10秒惩罚」，而非「超出实际截止时间的秒数」。

5. **无失败状态**：当前设计中无论得分多低都会进入结算页，没有明确的「游戏失败」概念，得0分也不会重新开始。

---

## 十二、后续扩展方向建议

基于当前代码架构，以下是较为自然的扩展位置：

### 12.1 关卡与内容扩展

- **新增关卡**：在 [levels.ts](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/data/levels.ts) 的 LEVELS 数组中添加即可，数据驱动无需改逻辑
- **新事件类型**：
  - 在 `EventType` 添加类型
  - 在 `tick()` 事件处理分支中添加效果
  - 在 EventNotification 和 ReplayPage 添加对应颜色/图标
  - 在 TutorialPage 添加说明
- **新工位名称**：扩展 `STATION_NAMES` 数组

### 12.2 游戏机制扩展

- **让优先级真正生效**：在 tick() 选取待执行任务时，先按 priority 排序（high > medium > low），同优先级内再按数组顺序，这是最核心的待补全逻辑
- **手动清理任务**：在 StationCard 中类似「触发补料」添加「清理」按钮，调用 addTask(stationId, 'clean')
- **增加并发数**：将 `maxParallelTasks = 2` 改为可配置（按关卡难度递增）
- **任务取消/中断**：支持取消进行中的任务，已消耗的进度作废
- **工人/资源概念**：引入工人数量限制、疲劳度等更复杂的调度维度
- **多波次/中场检查**：在一个关卡内设置多个检查点，不仅是最终结算

### 12.3 体验优化

- **任务排序自动按优先级**：当玩家修改优先级后，自动将高优先级任务排列到待执行列表前面
- **音效/动画反馈**：任务完成、事件触发时的音效和动效
- **更详细的教程/引导**：首次游戏时的新手引导步骤
- **快捷键操作**：键盘快捷暂停、快速切换选中任务等
- **游戏中实时得分预览**：显示当前各维度得分的预估情况

### 12.4 数据与统计扩展

- **更多统计维度**：平均任务等待时间、关键工位平均完成时间、事件响应速度等
- **成就系统**：基于历史数据的成就徽章（如「零失误通关」「闪电手速」等）
- **趋势图表**：在成绩页展示各次游玩的得分趋势曲线
- **导出/分享**：将成绩或复盘数据导出为图片/JSON

### 12.5 架构层面

- **useTheme hook**：[useTheme.ts](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/hooks/useTheme.ts) 已创建但未实现，可扩展深色/浅色主题切换
- **Home.tsx**：当前页面路由中没有使用，可能是早期设计残留，可考虑清理或用于首页逻辑
- **组件拆分**：随着功能增加，ReplayPage.tsx（466行）可考虑拆分为更多子组件
- **测试**：当前项目没有测试文件，scoreCalculator 和 gameStore 的核心逻辑适合添加单元测试

---

## 十三、核心文件速查

| 需求 | 关键文件 |
|------|----------|
| 修改关卡配置 | [data/levels.ts](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/data/levels.ts) |
| 修改游戏核心逻辑 | [store/gameStore.ts](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/store/gameStore.ts) |
| 调整得分公式 | [utils/scoreCalculator.ts](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/utils/scoreCalculator.ts) |
| 修改类型定义 | [types/index.ts](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/types/index.ts) |
| 调整任务UI/拖拽 | [components/TaskList.tsx](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/components/TaskList.tsx) |
| 调整工位面板 | [components/StationPanel.tsx](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/components/StationPanel.tsx) |
| 事件通知样式 | [components/EventNotification.tsx](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/components/EventNotification.tsx) |
| 复盘页面 | [pages/ReplayPage.tsx](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/pages/ReplayPage.tsx) |
| 数据存储 | [utils/storage.ts](file:///Users/zhangxinyu/sunxidan/9999-gsb/0618/station-game/src/utils/storage.ts) |
